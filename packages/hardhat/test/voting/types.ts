export enum Gender {
  Male = 1,
  Female = 2,
}

export enum Continent {
  Asia = 1,
  Africa = 2,
  NorthAmerica = 3,
  SouthAmerica = 4,
  Europe = 5,
  Australia = 6,
  Antarctica = 7
}

export enum AggregateOp {
  COUNT,
  SUM,
  MIN,
  MAX,
  AVG
}

export enum PredicateOp {
  EQ,
  NE,
  GT,
  LT,
}
